# Copy this file to .env and fill in your real MySQL password.
# .env itself is git-ignored and will never be committed.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=online_shopping
